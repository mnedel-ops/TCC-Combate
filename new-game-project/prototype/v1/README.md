# Protótipo V1 arquivado

Implementação anterior do combate, preservada apenas para referência histórica.

- Não é a cena principal do projeto.
- Não é chamada pelo fluxo V2.
- Usa `AlchemonSheet` mutável e resultados em `Dictionary`.

Implementação ativa: `res://combat_refactored.tscn`, `CombatRules`,
`CombatState` e `CombatEvent`.
